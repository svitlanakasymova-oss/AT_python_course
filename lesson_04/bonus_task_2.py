"""Ви розробляєте інструмент для тестування, який працює з файлами тестового набору.
Вам потрібно написати функцію для перевірки формату файлів у деякому каталозі.
Задача:
1. Доробіть функцію check_file_format(список_файлів, розширення),
яка приймає на вхід список_файлів та рядок розширення (наприклад, '.txt').
2. Функція повинна перевірити всі файли у списку і повернути новий список файлів, які мають вказане розширення.
Порада: використовуйте .endswith()"""

def check_file_format(file_list: list, extention: str):
    new_list = []
    for file in file_list:
        if file.endswith(extention):
            new_list.append(file)
    return new_list